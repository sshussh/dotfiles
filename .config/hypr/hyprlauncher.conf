# ~/.config/hypr/hyprlauncher.conf

background_color = $background
text_color = $text
selected_text_color = $bright_text
selected_background_color = $alternate_base

# Add this line to force a visible change if colors are failing
border_size = 2
border_color = $accent 


# Ensure there are no conflicting options outside the ui {} block
# or any extra characters.