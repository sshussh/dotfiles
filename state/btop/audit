#!/usr/bin/env bash
set -euo pipefail
here=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
repo=$(CDPATH= cd -- "$here/../.." && pwd)
. "$repo/state/lib/mutable-files.sh"
mutable_audit "$here/btop.conf" "${XDG_CONFIG_HOME:-$HOME/.config}/btop/btop.conf"
echo "btop runtime state matches the snapshot"
