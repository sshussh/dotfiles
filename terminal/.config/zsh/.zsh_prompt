# oh-my-posh prompt
if command -v oh-my-posh >/dev/null 2>&1; then
  eval "$(oh-my-posh init zsh --config "${ZDOTDIR}/prompt.json")"
else
  # Minimal fallback if oh-my-posh is missing
  PROMPT='%F{blue}%~%f %(?.%F{magenta}.%F{red})❯%f '
fi
