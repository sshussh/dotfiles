// Portable Niri config. Machine-local bits (outputs, xkb) live in config.kdl.

config-notification {
    disable-failed
}

gestures {
    hot-corners {
        top-left
    }
}

// Transparent so Noctalia's wallpaper layer shows between windows.
layout {
    background-color "transparent"
}

// Draw Noctalia's wallpaper layer inside the overview backdrop.
layer-rule {
    match namespace="^noctalia-wallpaper$"
    place-within-backdrop true
}

overview {
    workspace-shadow {
        off
    }
}

environment {
    XDG_CURRENT_DESKTOP "niri"
}

hotkey-overlay {
    skip-at-startup
}

prefer-no-csd

screenshot-path "~/Pictures/Screenshots/Screenshot from %Y-%m-%d %H-%M-%S.png"

animations {
    workspace-switch {
        spring damping-ratio=0.80 stiffness=523 epsilon=0.0001
    }
    horizontal-view-movement {
        spring damping-ratio=0.85 stiffness=423 epsilon=0.0001
    }
    window-movement {
        spring damping-ratio=0.75 stiffness=323 epsilon=0.0001
    }
    window-resize {
        spring damping-ratio=0.85 stiffness=423 epsilon=0.0001
    }
    config-notification-open-close {
        spring damping-ratio=0.65 stiffness=923 epsilon=0.001
    }
    overview-open-close {
        spring damping-ratio=0.85 stiffness=800 epsilon=0.0001
    }
}

// Rounded corners for every window.
window-rule {
    geometry-corner-radius 10
    clip-to-geometry true
}
window-rule {
    match app-id=r#"^org\.gnome\."#
    draw-border-with-background false
}
window-rule {
    match app-id=r#"^org\.gnome\.Calculator$"#
    match app-id=r#"^blueman-manager$"#
    match app-id=r#"^org\.gnome\.Nautilus$"#
    match app-id=r#"^xdg-desktop-portal$"#
    open-floating true
}
window-rule {
    match app-id=r#"^steam$"# title=r#"^notificationtoasts_\d+_desktop$"#
    default-floating-position x=10 y=10 relative-to="bottom-right"
    open-focused false
}
window-rule {
    match app-id="zen"
    match app-id="com.mitchellh.ghostty"
    match app-id="kitty"
    draw-border-with-background false
}
window-rule {
    match app-id=r#"firefox$"# title="^Picture-in-Picture$"
    open-floating true
}
// Noctalia's own windows (settings, dialogs) float.
window-rule {
    match app-id=r#"^noctalia"#
    match app-id=r#"^org\.noctalia\."#
    open-floating true
}

debug {
    honor-xdg-activation-with-invalid-serial
}

// Alt+Tab window switcher (replaces the default Mod+Tab-only behaviour).
recent-windows {
    binds {
        Alt+Tab         { next-window scope="output"; }
        Alt+Shift+Tab   { previous-window scope="output"; }
        Alt+grave       { next-window filter="app-id"; }
        Alt+Shift+grave { previous-window filter="app-id"; }
    }
}

include "binds.kdl"

cursor {
    xcursor-theme "Bibata-Modern-Classic"
    xcursor-size 24
}
