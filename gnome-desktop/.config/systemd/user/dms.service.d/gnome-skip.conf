[Service]
# The packaged dms.service is WantedBy=graphical-session.target. On GNOME it
# would claim the notification bus from gnome-shell. Niri starts it via
# niri.service.d instead.
ExecCondition=/bin/sh -c 'case ":${XDG_CURRENT_DESKTOP:-}:" in *:GNOME:*) exit 1 ;; *) exit 0 ;; esac'
