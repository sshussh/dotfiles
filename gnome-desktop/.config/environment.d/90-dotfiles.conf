# Ensure user binaries (matugen helpers, local tools) are on the session PATH.
PATH=$HOME/.local/bin:$PATH
# Let Qt applications follow the same generated GTK palette in GNOME and Niri.
QT_QPA_PLATFORMTHEME=gtk3
